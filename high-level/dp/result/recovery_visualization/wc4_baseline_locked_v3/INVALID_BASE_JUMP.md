# Invalid visualization: base discontinuity

The door latch behavior is fixed here, but these trajectories still contain a
base-position jump after `push_door`. Use the `locked_base_v4` visualization.
